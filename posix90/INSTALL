1. Edit Makfile and adjust the location(s) where files get installed
2. make
3. su / make install

The texinfo documentation does not get inserted into (dir) automatically, you
need to do this by hand. The doc directory contains pdf/html versions you
might wish to copy by hand to your favourite location. 